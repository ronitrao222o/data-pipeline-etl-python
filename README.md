# data-pipeline-etl-python
This project implements a modular ETL (Extract, Transform, Load) pipeline using Python and SQLite. The pipeline ingests raw sales data, applies transformations, and loads the processed data into a relational database for analytics and reporting.
